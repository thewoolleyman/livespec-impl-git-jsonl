"""Smoke test for the freshly-bootstrapped livespec-impl-plaintext scaffold.

Exists so the starter coverage gate (100% line + branch) has something to
measure during the bootstrap commit. Replaced as real code lands in
subsequent Phase D leaves.
"""


def test_smoke() -> None:
    assert True
